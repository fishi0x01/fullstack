import 'package:flutter/material.dart';
import 'package:flutter_app/view/counter/counter_page.dart';

class MyApp extends MaterialApp {
  const MyApp({Key key}) : super(key: key, home: const CounterPage());
}